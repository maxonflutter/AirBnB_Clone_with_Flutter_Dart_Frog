import 'package:flutter/material.dart';

class BookingFiltersScreen extends StatefulWidget {
  const BookingFiltersScreen({super.key});

  @override
  State<BookingFiltersScreen> createState() => _BookingFiltersScreenState();
}

class _BookingFiltersScreenState extends State<BookingFiltersScreen> {
  @override
  Widget build(BuildContext context) {
    final textTheme = Theme.of(context).textTheme;

    return Scaffold(
      appBar: AppBar(),
      body: Container(),
    );
  }
}
